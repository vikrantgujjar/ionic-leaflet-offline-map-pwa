import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-leaflet-offline-map-pwa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
